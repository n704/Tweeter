# TWEETER BACKEND ROOT

Contains all general files

## Docker

Create a docker image.
during docker build, test is run and build fails if test fails.

## PIP

All the `pip` dependency are stored in `requirements.txt` file.
